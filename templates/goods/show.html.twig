{% extends 'base.html.twig' %}

{% block title %}Goods{% endblock %}

{% block body %}
    <h1>Goods</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ good.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('goods_index') }}">back to list</a>

    <a href="{{ path('goods_edit', {'id': good.id}) }}">edit</a>

    {{ include('goods/_delete_form.html.twig') }}
{% endblock %}